<!-- Views/QR_Page.xaml -->
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyApp.ViewModels"
             x:Class="MyApp.Views.QR_Page"
             x:DataType="viewmodels:QR_PageViewModel"
             Title="Сканирование QR">

    <ContentPage.Content>
        <Grid>
            <!-- Индикатор загрузки -->
            <ActivityIndicator IsVisible="{Binding IsBusy}" 
                         IsRunning="{Binding IsBusy}"
                         VerticalOptions="Center"
                         HorizontalOptions="Center"/>
            <!-- Выбор помещения -->
            <StackLayout x:Name="StorageSelectionLayout" Padding="20" 
                             IsVisible="{Binding ShowStorageSelection}">
                <Label Text="Выберите помещение:" FontSize="Medium" />
                <Picker ItemsSource="{Binding StorageOptions}" 
                        SelectedItem="{Binding SelectedStorage}"
                        Title="Выберите помещение"
                        IsEnabled="{Binding IsNotBusy}" />
                <Button Text="Далее" 
                            Command="{Binding SelectStorageCommand}"
                            CommandParameter="{Binding SelectedStorage}" />
            </StackLayout>

            <!-- Выбор метода ввода -->
            <StackLayout x:Name="InputMethodLayout" Padding="20" 
                             IsVisible="{Binding ShowInputMethod}">
                <Label Text="Выберите способ ввода:" FontSize="Medium" />
                <Button Text="Ввод вручную" Command="{Binding ManualInputCommand}" />
                <Button Text="Сканировать QR код" Command="{Binding ScanQrCommand}" />
            </StackLayout>

            <!-- Ручной ввод -->
            <ScrollView x:Name="ManualInputLayout" 
                            IsVisible="{Binding ShowManualInput}">
                <StackLayout Padding="20">
                    <Label Text="Введите данные:" FontSize="Large" HorizontalOptions="Center" />

                    <Label Text="Наименование" />
                    <Entry Text="{Binding CurrentItem.Наименование}" />

                    <Label Text="Стеллаж" />
                    <Entry Text="{Binding CurrentItem.Стеллаж}" />

                    <Label Text="Полка" />
                    <Entry Text="{Binding CurrentItem.Полка}" />

                    <Label Text="Место" />
                    <Entry Text="{Binding CurrentItem.Место}" />

                    <Label Text="Количество фактич." />
                    <Entry Text="{Binding CurrentItem.Количество_фактич}" Keyboard="Numeric" />

                    <Label Text="Доп. описание" />
                    <Entry Text="{Binding CurrentItem.Доп_описание}" />

                    <Button Text="Сканировать QR" Command="{Binding ScanQrCommand}" Margin="0,20,0,0" />
                    <Button Text="Продолжить" Command="{Binding SaveAndContinueCommand}" />
                    <Button Text="Завершить" Command="{Binding SaveAndFinishCommand}" />
                </StackLayout>
            </ScrollView>
        </Grid>
    </ContentPage.Content>
</ContentPage>